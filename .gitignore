.gradle/
build/
/local.properties
*.apk
.idea/
*.iml

# Kotlin compiler scratch — build artifacts, and its crash logs embed absolute paths
.kotlin/

# Release signing. The keystore and its passwords live in ~/.android/ and in repo secrets,
# never here — losing the keystore means no installed user can ever be updated.
*.jks
*.keystore
keystore.properties
