package id.gauvin.grouse.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 type scale for now; kept as a named val so we can tune it later
// without touching the theme wiring.
val GooseTypography = Typography()
